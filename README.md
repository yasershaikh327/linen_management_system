# PHP Docker Template

- Do not overwrite or change `public/includes/config.inc.php` file.
- To activate AWS-S3 object storage, Un-comment them in `public/includes/config.inc.php` file.
- Copy `credentials.env.example` file and rename it to `credentials.env` and then update it as-per your local configuration.
